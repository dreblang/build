load http

server = http.createServer()

server.get('/hello', fn (req, res) {
  return 'hello world from dreblang!'
})

server.get('/hello/<world>', fn (req, res) {
  return 'hello world from dreblang! ' + req.params.world
})

server.listenAndServe(':9500')
